# CPU-only test/development dependencies for Cloud Agent (no NVIDIA GPU).
#
# This is the subset of app/requirements.txt that the Python test suite
# (tests/, run via `python -m unittest discover`) actually imports, pinned
# to the same versions as app/requirements.txt where those are pinned.
# The heavy GPU-only wheels from app/requirements.txt (flash-attn, xformers,
# sageattention, torchcodec, insightface, onnxruntime-gpu, etc.) are omitted
# because they require CUDA / an NVIDIA GPU that Cloud Agent VMs do not have.
#
# PyTorch itself is installed separately from the CPU index in .cursor/install.sh.

# CI's lightweight baseline (matches .github/workflows/ci.yml)
starlette==0.46.1
numpy==2.2.6
opencv-python-headless==4.12.0.88
Pillow==11.3.0
requests==2.32.4

# Test runner + JSON recovery used by the Director planner tests
pytest==8.3.5
tqdm==4.67.3
json_repair==0.59.5
websocket-client==1.9.0
psutil

# Core AI stack (CPU-importable; pinned to app/requirements.txt)
diffusers==0.36.0
transformers==4.57.1
tokenizers==0.22.1
accelerate==1.12.0
einops==0.8.2
mmgp==3.7.6
safetensors
huggingface_hub

# Vision / media helpers imported by the suite
iopath>=0.1.10
av==16.1.0
imageio==2.37.2
imageio-ffmpeg==0.6.0
timm==1.0.24
decord==0.6.0
pycocotools
scipy
ftfy==6.3.1
rembg==2.0.65
onnxruntime
ffmpeg-python
